#!/usr/bin/env ruby

require 'pathname'
$LOAD_PATH.unshift File.expand_path('../../lib', Pathname.new(__FILE__).realpath)

require 'builder'

$stdout.sync = true
Builder.new(url: ARGV[0], branch: ARGV[1]).build
